import { createContext } from 'react';

// Creamos el contexto que usaremos en la aplicacion
// Esto es como crear una especie de "caja" donde se guardaran losm datos a compartir en toda la SPA
export const CounterContext = createContext();
